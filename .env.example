# ─── AI ───────────────────────────────────────────────────────────────────────
# Required for voice tutor, retake quiz generation, and subjective quiz grading
GEMINI_API_KEY="your-gemini-api-key"

# ─── LiveKit voice tutor ───────────────────────────────────────────────────────
LIVEKIT_URL="wss://your-project.livekit.cloud"
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""
# Must match the agent worker registration (voice-agent)
LIVEKIT_AGENT_NAME=vidhyapika-tutor
VOICE_AGENT_SERVICE_SECRET="change-me-min-16-chars"
DEEPGRAM_API_KEY=""
HF_TOKEN=""

# ─── App ──────────────────────────────────────────────────────────────────────
APP_URL="http://localhost:3000"

# ─── Firebase ─────────────────────────────────────────────────────────────────
# Get these from your Firebase project settings > Service accounts > Generate new private key
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com"
# Paste the private_key value from the downloaded JSON (keep the newlines as \n)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"
# Format: your-project-id.appspot.com
FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Use a long random string (>= 32 chars), e.g.: openssl rand -base64 48
JWT_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS"

# ─── Email (Resend) ───────────────────────────────────────────────────────────
# API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxx
# Must be a verified sender/domain in Resend (e.g. notifications@yourdomain.com)
RESEND_FROM="Vidhyapika <notifications@yourdomain.com>"

# ─── Admin ────────────────────────────────────────────────────────────────────
# Email to receive flagged student alerts
ADMIN_EMAIL=admin@yourdomain.com

# Optional: Env-based Admin login (bypasses Firestore for admin only)
# If set, you can log in to the admin portal with these credentials.
ADMIN_LOGIN_EMAIL=admin@yourdomain.com
ADMIN_LOGIN_PASSWORD=change_me_min_8_chars

# Legacy (supported): if you already use ADMIN_PASSWORD for login
ADMIN_PASSWORD=change_me_min_8_chars
